{
  "name": "js-code-structure",
  "version": "0.0.2",
  "description": "Analyse the structure of your js project(relations between js files)",
  "bin": {"jcs": "bin/jcs.js"},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "timqian",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.2"
  },
  "devDependencies": {
    "mocha": "^2.2.5"
  }
}
